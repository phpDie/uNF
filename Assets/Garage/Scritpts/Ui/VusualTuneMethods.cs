using UnityEngine;
namespace Garage.Scritpts
{
    public class VusualTuneMethods : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
